import styled from 'styled-components';

export const Container = styled.div`
  padding: 5px;
  position: absolute;
  top: 0;
  left: 100%;
  transform: translateX(-300px);
  width: 300px;
  z-index: 100;
  max-height: 100vh;
  overflow: hidden;
`;
